<body>
    <main class="mt-5">
        <div class="container">
            <div class="card mx-auto shadow-lg border-0 card-background text-white" style="max-width: 500px;">
                <div class="card-header text-center bg-dark-transparent">
                    <img class="card-img-top mx-auto m-2 rounded-circle animate-img" src="https://cdn-icons-png.flaticon.com/512/929/929256.png" alt="Logo" style="width: 150px; height: 150px;">
                    <h5 class="text-uppercase p-4 animate-header">Crud para agregar personas para probar la API</h5>
                </div>
                <div class="card-body text-center bg-dark-transparent">
                    <p class="text-white animate-body">Con bootstrap, node.js, express y my sql</p>
                    <a href="/add" class="btn btn-dark animate-button">Iniciar</a>
                </div>
            </div>
        </div>
    </main>
</body>

<style>
    body {
      background-image: url('https://w.wallhaven.cc/full/k7/wallhaven-k71ljq.png');
      background-size: cover;
      background-position: center;
      font-family: 'Arial', sans-serif;
    }
    .btn-dark: hover{
        background-color: grey; /* Color del botón al pasar el ratón por encima */
      border-color: grey; /* Color del borde del botón al pasar el ratón por encima */


    }

         
    
    
    .card-background {
       background-image: url('https://w.wallhaven.cc/full/k7/wallhaven-k71ljq.png');
        background-size: cover;
        background-position: center;
        color: white;
    }
    .bg-dark-transparent {
        background-color: rgba(0, 0, 0, 0.7);
    }
    .animate-img {
        animation: bounce 1.5s infinite;
    }
    .animate-header {
        animation: fadeIn 2s ease-in;
    }
    .animate-body {
        animation: fadeIn 2.5s ease-in;
    }
    .animate-button {
        animation: pulse 2s infinite;
    }
    @keyframes bounce {
        0%, 20%, 50%, 80%, 100% {
            transform: translateY(0);
        }
        40% {
            transform: translateY(-30px);
        }
        60% {
            transform: translateY(-15px);
        }
    }
    @keyframes fadeIn {
        from {
            opacity: 0;
        }
        to {
            opacity: 1;
        }
    }
    @keyframes pulse {
        0% {
            transform: scale(1);
        }
        50% {
            transform: scale(1.1);
        }
        100% {
            transform: scale(1);
        }
    }
</style>
